@model Recommended_System.Models.Item_rate

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Item_rate</legend>

        @Html.HiddenFor(model => model.Id)

        <div class="editor-label">
            @Html.LabelFor(model => model.Up)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Up)
            @Html.ValidationMessageFor(model => model.Up)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Down)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Down)
            @Html.ValidationMessageFor(model => model.Down)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
